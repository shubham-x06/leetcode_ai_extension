import { useQuery } from '@tanstack/react-query'; import { getUserContest } from '../api/user'; import { useAuthStore } from '../store/useAuthStore'; export function useContest() { const token = useAuthStore((s) => s.token); return useQuery({ queryKey: ['contest'], enabled: !!token, queryFn: getUserContest, staleTime: 300000 }); }
