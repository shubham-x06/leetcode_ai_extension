import { useQuery } from '@tanstack/react-query'; import { getUserStats } from '../api/user'; import { useAuthStore } from '../store/useAuthStore'; export function useStats() { const token = useAuthStore((s) => s.token); return useQuery({ queryKey: ['stats'], enabled: !!token, queryFn: getUserStats, staleTime: 300000 }); }
