import React from 'react'; import { ProblemFilters } from '../components/problems/ProblemFilters'; export default function ProblemsPage() { const [filters, setFilters] = React.useState({ search: '' }); return <div><ProblemFilters filters={filters} setFilters={setFilters} /><div>Problem List</div></div>; }
